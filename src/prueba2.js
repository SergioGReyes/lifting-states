export const Utils = {
    toFarenheit: (cel) => {

    },
    toCelsius: (far) => {

    },
    randomIntFromInterval: (min, max) => {
        return Math.floor(Math.random() * (max - min + 1) + min);
    }
}